This app shows how we can convert a simple html file with react code (see `_react/index.html`) to the next.js
application. You can find simple HTML files with JS / React code in the `_react/` directory.
